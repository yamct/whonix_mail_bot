Make a file, bot.conf, based off of sample.conf and execute bot.py
For now IMAP is not supported, smtp and pop must use SSL.
The bot records the IDs of the email it forwards in the file sent, but
cannot delete any emails.
